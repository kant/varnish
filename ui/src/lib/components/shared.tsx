import * as React from 'react';
import styled from 'styled-components';

export const Spacer = styled.div`
    display: flex;
    flex-grow: 1;
`

export const Wrapping = styled.span`
    white-space: pre-wrap;
    word-wrap: break-word;
`;
